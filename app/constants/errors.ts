import maxCharacterCount from './maxCharacterCount'
import minCharacterCount from './minCharacterCount'

export default {
  toofew: `You have to select at least ${minCharacterCount} characters.`,
  toomany: `You can only select up to ${maxCharacterCount} characters.`,
}
